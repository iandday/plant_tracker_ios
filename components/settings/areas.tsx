import { ScrollView } from "react-native";
import { Text } from "~/components/ui/text";
export default function Areas() {
  return (
    <ScrollView>
      <Text>Areas</Text>
    </ScrollView>
  );
}
